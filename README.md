ansible-sample
==============

```
#login ansible-node
vagrant ssh ansible-node

# change user ansible
sudo su
su ansible

# ping client-node for ansible-playbook
ansible-playbook -i hosts ping.yml
```
